/**
 *
 *
 * => Recap Module 16
 */

// variable,
var myName = "Abdur Rahim";
var dream = "Web Developer";

console.log(`My Name is: ${myName}. ${dream} is my dream!`);

/**
 * How to Declear a variable
 * --------------------------
 *
 * => Must use "var, let, const" keyword to declear a variable.
 * => for variable name, Use single world,
 * => If your variable name is multi world then you should use camelCase
 * => not start with number
 * => [-] dash are not allow.
 */
