/**
 * In this file I will learn Js Break
 */

for (let i = 0; i < 10; i++) {
  if (i == 5) {
    break;
  }
  console.log(i);
}

/**
 * After finish the debugging. Now I know the break can break any statement.
 * The example above when [i==5] then the look is break
 * That's mean  loop is stop when loop execute the break statement
 */
