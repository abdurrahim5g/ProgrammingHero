/**
 * 
 * 
 * ৫. জাভাস্ক্রিপ্ট এ array এর map, forEach, filter, find কোনটা দিয়ে কি হয়।
      সেটার একটা সামারি লিখে ফেলো।
 */

const map = `In JS [map] method kind of loop. Map can loop all over the array. Map get a callBack function,
             With 3 paramiter, [ map((currentValue, index, array)=> {}) ]`;

const forEach = `In JS [ forEach ] same as {map} method. But forEach can't return anything.`;

const filter = `In JS [filter] is a another loop. But filter return a new array. Which condetion is true`;

const fine = `[Find] method similar with filter. But find method only return the first match value`;
